#include <stdio.h>
/**
 * main - reverse numbers.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 'Z'; i >= 'A'; i--)
	{
		putchar("%c", i);
	}
	putchar('\n');
	return (0);
}
